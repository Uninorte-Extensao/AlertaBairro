name: PR Checks

on:
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Lint
        run: echo "Ignorado para projeto HTML/JS nativo."

      - name: Checagem de tipos (TypeScript)
        run: echo "Ignorado para projeto HTML/JS nativo."

      - name: Build
        run: echo "Ignorado para projeto HTML/JS nativo."

      - name: Testes
        run: echo "Ignorado para projeto HTML/JS nativo."